export async function register() {
  // Define register to override Vercel's auto-generated one that fails to find Sentry configs
  // We can leave this empty or add custom instrumentation here later.
}
